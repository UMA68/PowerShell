# 暗号化文字列の復元ツール

鍵ファイルを使用して暗号化された文字列を復号し、元の平文を表示するPowerShellスクリプトです。

## 概要

このツールは、`Encryption.key` などの鍵ファイルを使用して暗号化された文字列を復号します。暗号化した文字列が何だったか忘れた場合の救済処置として使用します。

**復号結果の表示方法**

- 既定: **ポップアップダイアログのみ**（セキュリティ重視）
- オプション: `-ShowInConsole` を付けると **ターミナルにも表示**（周囲の目視に注意）

### 主な機能

- **安全な復号処理** - AES暗号化による鍵ベースの復号
- **GUIインターフェイス** - 使いやすいフォーム入力（Enter/Escapeキー対応）
- **オプションのターミナル出力** - `-ShowInConsole` 指定時のみコンソールに表示
- **二重起動防止** - 同時実行を防ぐ安全機構
- **自動クリーンアップ** - 処理後に機密情報をメモリから削除

## システム要件

### PowerShell

- **バージョン**: 5.1以上（推奨: 7.3.9以上）
- **実行ポリシー**: `RemoteSigned`または`Unrestricted`

### 前提条件

- 鍵ファイル（Encryption.key）が `Common/` フォルダーに存在すること
- 暗号化時と同じ鍵ファイルを使用する必要があります

## ディレクトリ構造

```Shell
暗号化文字列の復元/
├── Script/
│   ├── StringDecryption.ps1      # メインスクリプト
│   └── InputGUI.ps1              # GUI入力フォーム
├── Readme.md                     # このファイル
└── 暗号文字列の復号.ps1 - ショートカット.lnk
```

## 使い方

### 基本的な使用方法

1. **鍵ファイルの配置**
   - 暗号化時に使用した `Encryption.key` を `Common/` フォルダーに配置

2. **スクリプトの実行**
   - 「暗号文字列の復号 .ps1 - ショートカット」をダブルクリック
   - またはPowerShellで直接実行:

     ```powershell
     .\Script\StringDecryption.ps1
     ```

3. **文字列の入力**
   - 表示されたフォームに暗号化文字列を入力
   - 「復号実行」ボタンをクリック（またはEnterキーを押す）
   - キャンセルする場合は「キャンセル」ボタンまたはEscapeキーを押す

4. **結果の確認**
   - **既定:** ポップアップダイアログで表示（ターミナルには出力しません）
   - **任意:** `-ShowInConsole` を指定した場合のみターミナルにも表示できます（周囲の目視に注意）

### 出力例

- 既定（ポップアップのみ）: ダイアログに復号結果が表示されます（コンソール出力なし）
- `-ShowInConsole` 指定時:

```Shell
復号に成功しました。

=============================
復号結果: パスワード123
=============================

※ 上記の復号結果をコピーしてご利用ください
※ コンソールに表示されるため周囲の目視に注意してください
```

### パラメーター指定

```powershell
# カスタム鍵ファイルを使用
.\StringDecryption.ps1 -keyFileName "CustomKey.key"

# 復号結果をコンソールにも表示（周囲の目視に注意）
.\StringDecryption.ps1 -ShowInConsole
```

#### パラメーター

| パラメーター | 型 | デフォルト | 説明 |
|-----------|-----|-----------|------|
| `keyFileName` | string | "Encryption.key" | 使用する鍵ファイル名（Common フォルダー内） |
| `ShowInConsole` | switch | false | 復号結果をコンソールにも表示する（既定はポップアップのみ） |

## 処理フロー

### 1. 初期化フェーズ

- ディレクトリパスの構築
- 必要なスクリプトファイルの読み込み（InputGUI.ps1、NoDoubleActivation.ps1）

### 2. 前提チェックフェーズ

- 二重起動チェック
- 鍵ファイルの存在確認と読み込み

### 3. 入力フェーズ

- GUIフォームの表示
- 暗号化文字列の入力受付
- 入力検証（空文字列チェック）

### 4. 復号フェーズ

- SecureStringへの変換
- 平文への復号
- エラーハンドリング

### 5. 結果表示とクリーンアップ

- ターミナルへの復号結果表示（コピー＆ペースト用）
- 復号結果のポップアップ表示
- 機密情報のメモリからの削除

## トラブルシューティング

### よくあるエラー

| エラーメッセージ | 原因 | 対応方法 |
|-------------|------|--------|
| "鍵ファイル「XXX」が見つかりません" | 鍵ファイルが存在しない | `Common/` フォルダーに正しい鍵ファイルを配置 |
| "InputGUI.ps1 が見つかりません" | スクリプトファイルが存在しない | `Script/` フォルダーの構成を確認 |
| "文字列の復号に失敗しました" | 暗号文字列または鍵ファイルが不正 | 正しい暗号文字列と鍵ファイルを使用 |
| "すでに実行中です" | 同じスクリプトが実行されている | 既存プロセスの完了を待つ |

### よくある問題

#### 1. 復号に失敗する

**症状**: "文字列の復号に失敗しました" のエラーが表示される

**原因**:

- 暗号化時と異なる鍵ファイルを使用している
- 暗号文字列が破損している
- 暗号文字列のコピー時に改行や空白が混入している

**対応**:

- 暗号化時と同じ鍵ファイルを使用しているか確認
- 暗号文字列を正確にコピーペーストする
- 前後の空白や改行を削除する

#### 2. フォームが表示されない

**症状**: スクリプトを実行してもフォームが表示されない

**原因**:

- InputGUI.ps1が見つからない
- PowerShellのバージョンが古い

**対応**:

```powershell
# InputGUI.ps1 の存在確認
Test-Path ".\Script\InputGUI.ps1"

# PowerShell バージョン確認
$PSVersionTable.PSVersion
```

## セキュリティに関する注意

### 機密情報の取り扱い

- **画面表示**: 復号結果は画面に表示されます。作業時は周囲に注意してください
- **メモリクリーンアップ**: 処理終了時、自動的に機密情報をメモリから削除します
- **ログ出力なし**: 復号結果はログに記録されません（セキュリティ保護、`-ShowInConsole` 時のみコンソールに表示）

### 鍵ファイルの管理

- 鍵ファイルは厳重に管理してください
- 鍵ファイルを紛失すると復号できなくなります
- 鍵ファイルのバックアップを安全な場所に保管してください

### 使用上の注意

- 信頼できる環境でのみ実行してください
- 暗号文字列を第三者と共有しないでください
- 復号後は速やかに画面を閉じてください

## 関連スクリプト

| スクリプト | 場所 | 用途 |
|-----------|------|------|
| MakeEncryptedString.ps1 | 暗号化文字列の作成/ | 文字列を暗号化 |
| MakeEncrypted.ps1 | 暗号化鍵の作成/ | 鍵ファイルを生成 |
| InputGUI.ps1 | Script/ | GUI入力フォーム |
| NoDoubleActivation.ps1 | Common/ | 二重起動防止機構 |

## 実行例

### 標準的な使用

```powershell
# デフォルト鍵ファイルで復号
.\Script\StringDecryption.ps1
# 結果はポップアップに表示（コンソールには出力しません）

# フォームに暗号文字列を入力:
# 76492d1116743f0423413b16050a5345MgB8AEsAcwBzAF...

# 結果: パスワード123
```

### カスタム鍵ファイル使用

```powershell
# カスタム鍵で復号
.\Script\StringDecryption.ps1 -keyFileName "Production.key"
```

## よくある質問（FAQ）

### Q1: 復号に失敗する原因は？

**A**: もっとも多い原因は以下の3つです：

1. 暗号化時と異なる鍵ファイルを使用している
2. 暗号文字列が正確にコピーされていない（改行や空白の混入）
3. 暗号文字列自体が破損している

### Q2: 鍵ファイルを紛失した場合は？

**A**: 鍵ファイルを紛失すると復号できません。鍵ファイルは必ずバックアップを取り、安全な場所に保管してください。

### Q3: 複数の鍵ファイルを管理するには？

**A**: `-keyFileName` パラメーターで鍵ファイル名を指定できます：

```powershell
.\StringDecryption.ps1 -keyFileName "Project1.key"
.\StringDecryption.ps1 -keyFileName "Project2.key"
```

### Q4: 復号結果はログに残りますか？

**A**: いいえ。セキュリティ上の理由から、復号結果はログファイルに記録されません。結果はポップアップダイアログと実行中のターミナル画面でのみ表示されます。

### Q5: どのような暗号化方式を使用していますか？

**A**: AES（Advanced Encryption Standard）を使用しています。PowerShellの `ConvertFrom-SecureString` / `ConvertTo-SecureString` による標準的な暗号化です。

## バージョン履歴

### v1.2.2 (2026-01-20)

- `-ShowInConsole` オプションを追加（既定はポップアップのみ）
- 復号結果をコンソールに出す場合の注意文を追加

### v1.2.1 (2026-01-20)

- ScriptAnalyzer指摘対応（空catchのエラー出力、ヘルプコメント整備）
- COMオブジェクト解放失敗時のエラーハンドリング強化

### v1.2.0 (2025-12-11)

- クリーンアップ処理を関数化（Invoke-Cleanup）
- 二重起動チェックの早期実行とエラー時クリーンアップ強化
- 制御フローフラグ導入とスクリプトスコープ変数の整理
- COMオブジェクトのリソース解放を強化

### v1.1.0 (2025-12-09)

- ターミナルへの復号結果表示機能追加（コピー＆ペースト用）
- Enter/Escapeキー対応（GUI操作性向上）
- キャンセルボタン追加
- セキュリティクリーンアップ強化
- ガベージコレクション強制実行
- パラメーター検証強化
- エラー時のリソースクリーンアップ改善

### v1.0.0 (2025-12-09)

- 初版リリース
- PowerShell 5.1以上対応
- GUIフォーム入力
- 二重起動防止機構
- セキュアな機密情報削除
- 包括的なエラーハンドリング
- PowerShellヘルプドキュメント追加

---

**最終更新:** 2026年1月20日
