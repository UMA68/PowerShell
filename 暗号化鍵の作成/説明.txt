暗号化・復号化は標準でDPAPIが使用される。
ただ、ログオン機密やドメイン認証機密から鍵を生成しているので、
生成した自信の端末で無いと復号できない。

誰もが復号できる文字列の暗号化を実現するには、
ASEを利用する必要がある。

鍵(Key)が128,192,256bitが利用できるようなので、
真ん中の192bitの鍵を採用する。

実行すると、「Encryption.Key」を作成する。
(24バイトの配列。中身はランダム)

作成したファイルは、ReadOnlyに設定し、
うっかり書き潰さないようにしておくと良い。

以上。